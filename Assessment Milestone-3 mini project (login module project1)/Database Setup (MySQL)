CREATE DATABASE spring_login;

USE spring_login;

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) UNIQUE,
    password VARCHAR(10),
    empid VARCHAR(5) UNIQUE,
    email VARCHAR(100)
);
